name: "TeamsFx javascript CodeQL config"

queries:
  - name: Extended Security
    uses: security-extended

  - name: Security and Quality
    uses: security-and-quality

paths-ignore:
  - "**/*.test.ts"
  - "**/*.tests.ts"
