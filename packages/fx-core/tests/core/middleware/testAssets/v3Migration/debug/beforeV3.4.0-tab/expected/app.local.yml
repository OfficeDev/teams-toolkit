version: 1.0.0

deploy:
  - uses: prerequisite/install 
    with:
      devCert:
        trust: true

  - uses: cli/runNpmCommand
    with:
      args: install -D @microsoft/teamsfx-run-utils@alpha
      workingDirectory: .

