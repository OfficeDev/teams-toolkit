{
    "type": "AdaptiveCard",
    "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
    "version": "1.4",
    "body": [
        {
            "type": "TextBlock",
            "text": "${title}",
            "size": "large"
        },
        {
            "type": "TextBlock",
            "text": "${message}"
        }
    ]
}